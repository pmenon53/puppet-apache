{
  "puppet_task_version": 1,
  "supports_noop": false,
  "description": "Allows users to run apachectl commands through puppet",
  "parameters": {
     "command: {
        "decription" : "which apachectl command to run",
        "type" : "Enum[start,stop,restart,status,graceful,graceful-stop]"
     }
  }
}
